{
  "name": "timetrack-backend",
  "version": "1.0.0",
  "description": "Sistema de control de asistencia y gestión de empleados con Express.js y PostgreSQL",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "build": "echo 'No build step required for Node.js'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "health": "node -e \"require('http').get('http://localhost:4000/health', (res) => { let data = ''; res.on('data', (chunk) => data += chunk); res.on('end', () => console.log(data)); });\"",
    "db:create": "node scripts/create-tables.js",
    "db:seed": "node scripts/seed-users.js",
    "db:setup": "npm run db:create && npm run db:seed"
  },
  "keywords": [
    "timetrack",
    "asistencia",
    "express",
    "postgresql",
    "sequelize"
  ],
  "author": "TimeTrack Team",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "dotenv-safe": "^9.1.0",
    "express": "^5.1.0",
    "express-rate-limit": "^8.2.1",
    "express-validator": "^7.3.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.1",
    "pg": "^8.16.3",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.7",
    "winston": "^3.18.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
